# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    app.path.boites_images: /uploads/images/boites
    app.path.partners_images: /uploads/images/partners
    app.path.itemGroup_images: /uploads/images/itemGroup
    app.path.item_images: /uploads/images/item
    app.path.quote_request_lines_images: /uploads/images/quote_request_images

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones

    # Corrige l'extension des vignettes LiipImagine quand un filtre change le format
    # (ex: exemple.png -> exemple.webp) : sans ça le fichier reste nomme .png sur le
    # disque et Apache sert un Content-Type image/png alors que les octets sont du webp.
    app.imagine.cache.format_extension:
        class: Liip\ImagineBundle\Imagine\Cache\Resolver\FormatExtensionResolver
        arguments:
            - '@liip_imagine.cache.resolver.default'
            - '@liip_imagine.filter.configuration'
        tags:
            - { name: 'liip_imagine.cache.resolver', resolver: 'format_extension' }
